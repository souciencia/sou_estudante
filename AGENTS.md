# AGENTS.md — SoU_Estudante

- ATTENTION: DONT'T COMMIT SECRETS (`ELASTIC_PASSWORD`, `ELASTICSEARCH_APIKEY`, etc).
- ATTENTION: DON'T READ `packages/bulker/data/dados.csv`.
- Whenever I ask to read a task file, look in the folder `docs/tasks/` for `.md` file for a equivalent. Only read a task file correspondent.
