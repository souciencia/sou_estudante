# AGENTS.md — SoU_Estudante

- ATENÇÃO: NÃO commitar segredos (`ELASTIC_PASSWORD`, `ELASTICSEARCH_APIKEY`) em nenhum arquivo versionado.
- ATENÇÃO: NÃO leia o arquivo`packages/bulker/data/dados.csv`.
