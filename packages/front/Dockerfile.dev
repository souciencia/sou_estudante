FROM node:24-alpine
 
RUN apk add --no-cache \
    git \
    curl
 
WORKDIR /app
COPY package*.json ./
RUN npm install
 
EXPOSE 3000
CMD ["npx", "next", "dev", "--hostname", "0.0.0.0", "--port", "3000"]
