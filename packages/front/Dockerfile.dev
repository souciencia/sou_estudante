FROM node:24-alpine
 
RUN apk add --no-cache \
    git \
    curl
 
WORKDIR /app
COPY package-lock.json package.json ./
RUN npm ci
 
EXPOSE 3000
CMD ["npx", "next", "dev", "--hostname", "0.0.0.0", "--port", "3000"]
