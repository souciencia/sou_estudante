
# Frontend (`front/`)

## Core Tech Stack
- Framework: Next.js (App Router)
- Styling: Tailwind CSS
- Formatting and linting: Biome — run `npx biome check --write .` before finalizing any changes.
- Test Runner: Vitest
- Render: React Testing Library (RTL) + jsdom